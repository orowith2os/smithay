window.SIDEBAR_ITEMS = {"enum":[["TransientSource","A [`TransientSource`] wraps a Calloop event source and manages its registration. A user of this type only needs to perform the usual Calloop calls (`process_events()` and `*register()`) and the return value of `process_events()`."]]};